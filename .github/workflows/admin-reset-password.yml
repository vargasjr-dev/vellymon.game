name: Admin - Reset Password

on:
  workflow_dispatch:
    inputs:
      email:
        description: "User email address"
        required: true
        type: string
      new_password:
        description: "New password (min 8 characters)"
        required: true
        type: string

jobs:
  reset-password:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Pull environment variables from Vercel
        run: vercel env pull .env.local --yes --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: <redacted type="High-Entropy Base64 Token" />
          VERCEL_PROJECT_ID: prj_bAckE5PGlllJHSE2kGL74Bq3MjPV

      - name: Reset password
        run: |
          set -a
          source .env.local
          set +a
          node scripts/reset-password.mjs '${{ inputs.email }}' '${{ inputs.new_password }}'
